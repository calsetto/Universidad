@model Universidad.Models.Universidad
@{
    ViewBag.Title = "Eliminar";
}

<h1>Eliminar</h1>

<h3>Seguro que desea eliminar a la universidad?</h3>
<div>
    <h4>Universidad</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.IDuniversidad)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.IDuniversidad)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Nombre)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Nombre)
        </dd>
    </dl>

    <form asp-action="Eliminar">
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Lista">Volver a la lista</a>
    </form>
</div>

